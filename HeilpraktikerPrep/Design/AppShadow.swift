import SwiftUI

/// Shadow tokens — subtle elevation for cards and modals.
enum AppShadow {
    static let cardColor = Color.black.opacity(0.06)
    static let cardRadius: CGFloat = 10
    static let cardY: CGFloat = 4

    static let modalColor = Color.black.opacity(0.18)
    static let modalRadius: CGFloat = 24
    static let modalY: CGFloat = 10
}
