import Foundation

/// Corner radius scale — "Standard" altitude (8–16pt, hero at 24).
enum AppRadius {
    static let chip: CGFloat = 10
    static let row: CGFloat = 12
    static let button: CGFloat = 14
    static let card: CGFloat = 16
    static let hero: CGFloat = 24
}
